# Environment Variables — ADF Automation Scripts
# ===============================================
# Works with:
#   - adf_report_generator.py        (pass --env flag)
#   - adf_linked_service_inventory.py (pass --env flag)
#   - adf_trigger_raw_extractor.py   (always uses env vars)
#
# HOW TO USE:
#   1. Copy this file:  cp .env.example .env
#   2. Fill in your real values in .env
#   3. NEVER commit .env to Git — it is in .gitignore
#
# ── ADF scripts (report generator + linked service inventory) ──────────────
AZURE_SUBSCRIPTION_ID=your-subscription-id-here
AZURE_RESOURCE_GROUP=your-resource-group-here
ADF_FACTORY_NAME=your-adf-factory-name-here

# ── Trigger raw extractor (Legacy environment) ─────────────────────────────
LEGACY_SUBSCRIPTION_ID=your-legacy-subscription-id-here
LEGACY_RESOURCE_GROUP=your-legacy-resource-group-here
LEGACY_FACTORY_NAME=your-legacy-adf-factory-name-here

# ── Trigger raw extractor (Migrated environment) ───────────────────────────
MIGRATED_SUBSCRIPTION_ID=your-migrated-subscription-id-here
MIGRATED_RESOURCE_GROUP=your-migrated-resource-group-here
MIGRATED_FACTORY_NAME=your-migrated-adf-factory-name-here

# ── Azure authentication (only needed for Service Principal auth) ───────────
# Leave blank if using 'az login' or Managed Identity
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=

# ── Optional: override output timezone for adf_report_generator.py ─────────
# Uses IANA timezone names — e.g. Asia/Kolkata, America/New_York, UTC
TARGET_TIMEZONE=Australia/Sydney
