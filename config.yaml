# ADF Report Generator — Configuration File
# ==========================================
# Copy this file and fill in your values.
# NEVER commit the filled-in version to Git.
# Add config.yaml to your .gitignore.

# Your Azure subscription ID
# Find it: az account show --query id -o tsv
subscription_id: YOUR_SUBSCRIPTION_ID_HERE

# Resource group containing your Azure Data Factory
resource_group: YOUR_RESOURCE_GROUP_HERE

# Name of your Azure Data Factory instance
factory_name: YOUR_ADF_FACTORY_NAME_HERE
